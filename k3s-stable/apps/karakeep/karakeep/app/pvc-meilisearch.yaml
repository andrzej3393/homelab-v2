apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: meilisearch
  namespace: karakeep
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi
  storageClassName: nfs-ssd
